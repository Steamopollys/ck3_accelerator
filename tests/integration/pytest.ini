[pytest]
# Register the custom mark so pytest does not warn about it.
# "integration" marks tests that require a live CK3 install (opt-in, not CI).
markers =
    integration: manual/opt-in tests that launch the real CK3 (never run in CI)
